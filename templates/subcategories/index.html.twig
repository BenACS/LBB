{% extends 'base.html.twig' %}

{% block title %}Hello SubcategoriesController!{% endblock %}

{% block body %}
	<div class="container-fluid d-flex flex-row col-12">
		<!-- Filters -->
		<div class="container-fluid col-2 text-center">
			<div>
				<p>Filter</p>
			</div>
			<div>
				{{ form_start(form) }}

				{{ form_end(form) }}
			</div>
		</div>

		<!-- Products -->
		<div class="container col-8 d-flex flex-row flex-wrap">
			{% for i in range(0, products|length - 1) %}
				<div class="col-4 text-center">                                 
                    <div class='text-center border p-2' style='height:85%'>
                        <img class="img-fluid" src="{{ articleImages[i] }}" alt="" width="100%">
                        <h4 class="pb-1">{{ products[i].title }}</h4>
                    </div>                               
                    <a class="btn buttonColor mt-2" href="">View</a>
                </div>
			{% endfor %}
		</div>
	</div>
{% endblock %}
