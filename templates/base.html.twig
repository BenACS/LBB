<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="stylesheet" href="https://use.fontawesome.com/releases/v5.2.0/css/all.css" integrity="sha384-hWVjflwFxL6sNzntih27bfxkr27PmbbK/iSvJ+a4+0owXq79v+lsFkW54bOGbiDQ" crossorigin="anonymous">
        <meta name="google-signin-client_id" content="test-e-commerce-270114.apps.googleusercontent.com">
        {% block stylesheets %}
            {{ encore_entry_link_tags('app') }}
        {% endblock %}
        <link href="https://stackpath.bootstrapcdn.com/font-awesome/4.7.0/css/font-awesome.min.css" rel="stylesheet" integrity="sha384-wvfXpqpZZVQGK6TAh5PVlGOfQNHSoD2xbE+QkPxCAFlNEevoEH3Sl0sibVcOQVnN" crossorigin="anonymous">
    </head>
    <body style="overflow-x: hidden;overflow-y:auto;">
        <header class="position-fixed w-100" style="z-index:80">
           <nav class="navbar navbar-expand-sm navbar-dark flex-column p-0" style="background-color: #000000">
                <div class="col-12">
                    <div class="row justify-content-between">
                        <!-- Logo -->
                        <div class="col-lg-2 col-sm-2 my-1">
                            <a class="navbar-brand" href="{{ path('home') }}"><img src="https://cdn.discordapp.com/attachments/682142335601344612/682865705917349908/Logo.png" alt="logo" width="20%"></a>
                        </div>

                        <!-- Search bar -->
                        <div class="col-lg-8 col-sm-8 justify-content-center my-2">
                            <form class="col-lg-12 form-inline my-2 my-lg-0 d-flex justify-content-center">
                                <input class="col-lg-6 col-sm-9 form-control mr-sm-2" type="text" placeholder="Search">
                                <button class="col-lg-2 col-sm-2 btn btn-secondary my-2 my-sm-0" type="submit">Search</button>
                            </form>
                        </div>

                        <!-- Buttons -->
                        <div class="col-lg-2 col-sm-2 collapse navbar-collapse justify-content-end" id="navbarColor02">
                            <ul class="navbar-nav">
                                <li class="nav-item mr-5">
                                    <a class="nav-link" href="{{ path('authentification') }}"><i class="fas fa-user-alt"></i></a>
                                </li>
                                <li class="nav-item ml-5 mr-3">
                                    <a class="nav-link" href="#"><i class="fas fa-shopping-cart"></i></a>
                                </li>
                            </ul>
                        </div>
                    </div>

                    <div class="row py-2" style="background-color: #696969">
                        <ul class="navbar-nav col-12 justify-content-around">
                            {% for categories in categories %}
                                {% if categories.parentId == 0 %}
                                    <li class="nav-item categories">
                                        <a class="nav-link" href="{{ path('category', {category: categories.categoryName, id: categories.id})}}">{{ categories.categoryName }}</a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                    </div>

                    <div class="row disNone subCategories py-2" style="background-color: #0080ff">
                        <ul class="navbar-nav col-12 justify-content-around">
                            {% for categories in categories %}
                                {% if categories.parentId == 1 %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="#">{{ categories.categoryName }}</a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                    </div>

                    <div class="row disNone subCategories py-2" style="background-color:#0080ff">
                        <ul class="navbar-nav col-12 justify-content-around">
                            {% for categories in categories %}
                                {% if categories.parentId == 2 %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="#">{{ categories.categoryName }}</a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                    </div>

                    <div class="row disNone subCategories py-2" style="background-color:#0080ff">
                        <ul class="navbar-nav col-12 justify-content-around">
                            {% for categories in categories %}
                                {% if categories.parentId == 3 %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="#">{{ categories.categoryName }}</a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                    </div>

                    <div class="row disNone subCategories py-2" style="background-color:#0080ff">
                        <ul class="navbar-nav col-12 justify-content-around">
                            {% for categories in categories %}
                                {% if categories.parentId == 4 %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="#">{{ categories.categoryName }}</a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                    </div>

                    <div class="row disNone subCategories py-2" style="background-color:#0080ff">
                        <ul class="navbar-nav col-12 justify-content-around">
                            {% for categories in categories %}
                                {% if categories.parentId == 17 %}
                                    <li class="nav-item">
                                        <a class="nav-link" href="#">{{ categories.categoryName }}</a>
                                    </li>
                                {% endif %}
                            {% endfor %}
                        </ul>
                    </div>
                </div>
            </nav>
        </header>
        <main style="padding-top:120px;padding-bottom:120px;">
            {% block body %}{% endblock %}
        </main>
        <footer id="myfooter" class="page-footer font-small blue pt-2 border-top" >
            <!-- Contact us -->
            <div class="text-center" >
                <a href="#" style="color: black">Contact us</a>
            </div>

            <!-- Terms of use etc -->
            <div class="d-flex justify-content-center">
                <ul class="list-unstyled d-flex justify-content-between">
                    <li class="mx-1"><a href="#" style="color: black">Terms of use |</a></li>
                    <li class="mx-1"> <a href="#" style="color: black">Conditions of sale |</a></li>
                    <li><a href="#" style="color: black">Legal notice</a></li>
                </ul>
            </div>

            <!-- Copyright -->
            <div class="footer-copyright text-center pt-2 pb-2 border-top" style="background-color: rgb(200,200,200);">
                <span class="font-weight-bold">
                    © <span style="color: gray">Copyright</span>
                </span>
                <a href="#"> LBB "Les Bons Booléens"</a>
            </div>
        </footer>
        {{ encore_entry_script_tags('app') }}
        {% block javascripts %}{% endblock %}
    </body>
</html>
