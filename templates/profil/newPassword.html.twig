{% extends 'base.html.twig' %}

{% block title %}Profile Page
{% endblock %}

{% block body %}


	<div class="col-lg-6 col-md-8 col-sm-8 col-10 mx-auto ">
		<div class="pt-3">
			<div class="text-center">
				<h2 class="mt-1">Edit your password</h2>
			</div>

			{{ form_start(form)}}
			<div id="stepOne">
				{{ form_row(form.password , { 'label':'Password','attr': {'placeholder': 'your password ...'}})}}
				{{ form_row(form.confirmPassword , { 'label':'Confirm Password','attr': {'placeholder': 'Confirm your password ...'}})}}
				<div class="text-center">
					<button type="submit" class="btn btn-success">Confirm</button>
				</div>
			</div>
		</div>

		{{ form_end(form)}}
		<!-- <form method="post" action="{{path('newPassword')}}">
			<div class="form-group">
				<label>New password</label>
				<input type="password" class="form-control" type="text" name="newPassword">
			</div>
			<div class="form-group">
				<label>Confirm your password</label>
				<input type="password" class="form-control" type="text" name="confirmNewPassword">
			</div>
			<button type="submit" class="btn btn-success">Confirm</button>
		</form> -->
	</div>
</div>{% endblock %}
